aiofiles==25.1.0
aiogram==3.30.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.2
aiosignal==1.4.0
annotated-types==0.7.0
attrs==26.1.0
certifi==2026.7.22
frozenlist==1.8.0
idna==3.18
magic-filter==1.0.12
multidict==6.7.1
openai>=2.0.0
propcache==0.5.2
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
python-socks[asyncio]>=2.4.3
typing-inspection==0.4.2
typing_extensions==4.16.0
yarl==1.24.5
aiosqlite
curl_cffi
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
# BrowserProvider: Python package only. Chromium binaries are a separate step.
# Local:  python -m playwright install chromium
# Amvera Python-pip: pip installs this file at BUILD; browsers are NOT downloaded.
#   At START (same container, before uvicorn/bot): sh scripts/install_playwright.sh
#   Cache binaries on persistent disk: PLAYWRIGHT_BROWSERS_PATH=/data/ms-playwright
#   Debian libs (if Chromium crashes on missing .so): python -m playwright install-deps chromium
#   (~200–400 MB; first start is slow. Do not put browsers under /data if installing at Docker BUILD.)
playwright>=1.40.0

